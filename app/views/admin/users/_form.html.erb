<%= simple_form_for user, :url => admin_users_path, :method => :post  do |f| %>
	
	<%= f.input :email %>
	
	<%= f.input :password %> 

	<%= f.input :admin, label: "Is an admin?" %>

	<%= f.submit %> 
	
<% end %>